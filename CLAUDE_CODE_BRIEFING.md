# 📋 תדריך מלא — פרויקט Stock Scout

> **הוראה לקלוד:** זה תדריך שמסביר את כל הפרויקט. קרא אותו במלואו, ואז קרא את הקבצים בפועל ב-repo (`scanner.py`, `pages/index.js`, `.github/workflows/weekly-scan.yml`). אל תתחיל לכתוב קוד עד שביקשתי ממך — קודם תוודא שהבנת, ותשאל אותי מה לא ברור. אני המשתמש, מדבר עברית, ועבדתי עד היום בשיטת העתק-הדבק דרך הדפדפן ב-GitHub. אני לא מתכנת מנוסה — תסביר לי דברים בפשטות.

---

## 🎯 מה זה Stock Scout

מערכת אוטומטית שסורקת כל יום ראשון את כל שוק המניות האמריקאי, מוצאת את 20 המניות שעלו הכי הרבה באותו שבוע (שישי-לשישי), אוספת "באז" (שיח חברתי) מ-Reddit ו-StockTwits לכל מנייה, מחשבת ציון באז 1-10, ושולחת מייל + מעדכנת דשבורד.

**בניתי את זה עבור הבוס שלי** (משקיע). הוא ראה את המערכת, מאוד מרוצה, ונתן לי רשימת שיפורים גדולה — זו המשימה עכשיו.

---

## 🏗️ ארכיטקטורה — איך הכל מחובר

| רכיב | מה הוא עושה | איפה |
|---|---|---|
| **scanner.py** | המוח. כל הלוגיקה של הסריקה | שורש ה-repo |
| **pages/index.js** | הדשבורד (Next.js / React) | תיקיית pages |
| **.github/workflows/weekly-scan.yml** | מתזמן — מריץ את scanner.py כל יום ראשון | תיקיית workflows |
| **pages/api/trigger-scan.js** | endpoint שכפתור "הפעל סריקה" בדשבורד קורא לו | תיקיית pages/api |
| **Supabase** | מסד נתונים. טבלה `weekly_scans` | שירות ענן |
| **Vercel** | מארח את הדשבורד | שירות ענן — stock-scout-phi.vercel.app |
| **Resend** | שולח את המיילים | שירות ענן |
| **Apify** | גורף נתונים מ-Reddit ו-StockTwits (בתשלום) | שירות ענן |

**זרימה:** GitHub Actions (יום ראשון) → מריץ scanner.py → סורק עם yfinance → מסנן לפי מרקט קאפ → 20 מניות → Apify אוסף באז → מחשב ציונים → שומר ב-Supabase → שולח מייל ב-Resend → הדשבורד ב-Vercel קורא מ-Supabase ומציג.

---

## 📦 מבנה מסד הנתונים (Supabase)

טבלה אחת: `weekly_scans`
- `week_label` — טקסט, למשל `"01.05-08.05.2026"`
- `stocks_json` — JSON כמחרוזת. מבנה: `{"stocks": [...], "bonus": [...]}`
- `created_at` — timestamp

כל סריקה = שורה אחת בטבלה. הדשבורד קורא את כל השורות ומציג אותן (יש בורר שבועות בראש העמוד).

---

## 🔑 GitHub Secrets (כבר מוגדרים — אל תיגע אלא אם אבקש)

`SUPABASE_URL`, `SUPABASE_SECRET_KEY`, `RESEND_API_KEY`, `BOSS_EMAIL`, `MIN_MARKET_CAP`, `DASHBOARD_URL`, `APIFY_TOKEN`, `GH_TOKEN`, `GH_USERNAME`

---

## 📄 מבנה scanner.py — הפונקציות העיקריות

- `get_two_fridays()` — מחזיר את שני ימי שישי האחרונים (טווח השבוע)
- `get_week_label()` — בונה את תווית השבוע מהתאריכים
- `get_ticker_universe()` — מחזיר רשימת כל הטיקרים בשוק האמריקאי + מילון שמות
- `fetch_weekly_changes(tickers)` — מביא מחירי שישי-לשישי דרך yfinance, מחשב % שינוי
- `find_top20_by_marketcap(price_data, names_dict)` — עובר על המניות שעלו מהגבוה לנמוך, מסנן לפי `MIN_MARKET_CAP`, עוצר ב-20
- `apify_run_actor(...)` — מריץ actor של Apify (פולינג חכם, עוצר כשמגיע ליעד)
- `fetch_reddit_buzz_apify_batch(tickers, names_dict)` — מביא פוסטים מ-Reddit
- `fetch_stocktwits_apify_batch(tickers)` — מביא הודעות מ-StockTwits
- `calculate_buzz_score_v2(...)` — מחשב ציון באז 1-10
- `build_buzz_from_data(...)` — בונה את אובייקט הבאז המלא לכל מנייה
- `get_previous_week_data()` — קורא את הסריקה הקודמת מ-Supabase (בשביל streak)
- `save_to_supabase(stocks, bonus, week_label)` — שומר סריקה
- `send_email(stocks, bonus, week_label)` — שולח מייל דרך Resend
- `main()` — מריץ הכל בסדר

**איך עובד ציון הבאז (calculate_buzz_score_v2):** משקלל פוסטים יחסית למרקט קאפ. Reddit שווה פי 3 מ-StockTwits. `weighted = (Reddit×3) + StockTwits`, ואז `per_billion = weighted / market_cap_in_billions`. סקאלה: ככל שיותר באז יחסי לגודל החברה → ציון גבוה יותר. יש בונוס למילות מפתח (takeover, FDA, partnership וכו'), ויש תקרות (אם אין Reddit בכלל → מקסימום 6).

---

## ✅ מה כבר עובד (אל תשבור!)

- הסריקה השבועית רצה ומביאה 20 מניות אמיתיות
- הבאז מ-Reddit + StockTwits עובד (20 פוסטים Reddit, 40 הודעות StockTwits לכל מנייה)
- הדשבורד מציג טבלה, חלונית פרטים, סנטימנט, ציטוטים, התראות באז
- מצב כהה/בהיר, עברית/אנגלית, בורר שבועות
- שמירה ל-Supabase עובדת

---

## 🐞 באגים ידועים (לא לתקן עכשיו — הבוס אמר "עזוב")

1. **מייל ל-2 כתובות** — Resend לא מקבל מחרוזת עם פסיקים בשדה `to`. צריך לפצל ל-array. **דחוי.**
2. **cron אוטומטי** — לא רץ אוטומטית ביום ראשון האחרון (רץ ידנית). **דחוי.**

---

# 🚀 המשימה — מה הבוס ביקש

## חלק א' — תיקונים קטנים

### 1. מרקט קאפ מינימלי → 250 מיליון
כרגע 500 מיליון. צריך לשנות ל-**250,000,000**. זה ה-secret `MIN_MARKET_CAP` ב-GitHub (אני אעדכן אותו ידנית — רק תגיד לי). ודא שאין מספר 500 מיליון תקוע קשיח (hardcoded) בקוד.

### 2. צבעי הרמזור — לשנות את הלוגיקה
כרגע הצבע בעמודת המגמה מבוסס על **רצף שבועות** (streak). הבוס רוצה שיהיה מבוסס על **אחוז ההופעות מתוך כלל הסריקות**:
- 🔴 **אדום** — המנייה הופיעה ב-0-30% מהסריקות (או: פחות מ-4 פעמים)
- 🟡 **צהוב** — הופיעה ב-30-70% מהסריקות
- 🟢 **ירוק** — הופיעה במעל 70% מהסריקות
- בנוסף: צריך להציג את **המספר בפועל** — כמה פעמים הופיעה (למשל "ירוק · 7 הופעות")

---

## חלק ב' — השינוי הגדול: 9 סריקות היסטוריות (Backfill)

הבוס לא רוצה לחכות חודשיים שייצבר מידע. הוא רוצה ללכת **אחורה**:
- לבנות **8 סריקות היסטוריות**: 4 שבועות של מרץ + 4 שבועות של אפריל
- **+ הסריקה של מאי** שכבר קיימת = **9 סריקות סה"כ**
- הסריקות ההיסטוריות **בלי באז** — רק מחיר, % עלייה, מרקט קאפ (yfinance חינמי, חוסך כסף ב-Apify)
- אחרי שיש 9 סריקות → לזהות את **20 המניות שהופיעו הכי הרבה פעמים** → ועליהן בלבד להריץ באז

**ההחלטה שהתקבלה איך לבנות:** לשנות את `scanner.py` שיתמוך ב**מצב היסטורי** — כלומר שאפשר להריץ אותו על תאריך בעבר. כך אפשר להריץ אותו 8 פעמים (אחת לכל שבוע היסטורי) והוא יבנה את הסריקה לאותו שבוע.

נקודות טכניות לבדוק/לטפל:
- `get_two_fridays()` כרגע מבוסס על `datetime.now()` — צריך לאפשר להעביר תאריך כפרמטר
- yfinance **כן** יודע להביא מחירים היסטוריים של מרץ-אפריל — זה אפשרי
- במצב היסטורי — לדלג על כל קריאות ה-Apify (אין באז)
- צריך מנגנון להריץ את 8 הסריקות (אפשר ידנית אחת-אחת, או לולאה)
- חשוב שלא יהיו כפילויות ב-Supabase (לבדוק אם week_label כבר קיים)

---

## חלק ג' — חלונית הפרטים: מעקב מלא + רוחב דינמי

זה ה**הכי חשוב לבוס** — שקיפות מלאה.

כשלוחצים על מנייה, החלונית צריכה להציג את **כל ההיסטוריה** של אותה מנייה:
- כל שבוע שבו היא הופיעה ב-TOP 20
- לכל שבוע: התאריך, אחוז העלייה באותו שבוע, הדירוג באותו שבוע
- למשל: מנייה שהופיעה בשבוע 2 של מרץ, שבוע 4 של מרץ, שבוע 3 של אפריל, ושבוע 1 של מאי — צריך לראות את כל 4 השורות עם הנתונים

**רוחב/גודל דינמי — קריטי:**
- החלונית צריכה להתאים את גודלה ל**מספר השבועות בפועל** של אותה מנייה
- מנייה שהופיעה **פעמיים** → חלונית קומפקטית עם 2 שורות. לא תיראה ריקה.
- מנייה שהופיעה **9 פעמים** → החלונית מתרחבת ומציגה 9 שורות (עם גלילה פנימית אם צריך)
- **לא** רוחב קבוע גדול שבנוי ל-9 ומנייה עם 2 הופעות נראית בו אבודה
- דינמי — מתאים לתוכן. לא סטטי-גדול.

(הערה: בקוד הנוכחי של index.js יש כבר רכיב `StockPanel` עם סקציית "APPEARANCE HISTORY" — צריך להרחיב/לתקן אותו)

---

## חלק ד' — באז רק על המניות הכי תכופות

אחרי שיש 9 סריקות: להריץ באז (Apify) **רק על 20 המניות שהופיעו הכי הרבה פעמים** מתוך כל 9 הסריקות. לא על מניות חדשות אקראיות — רק על אלה שמראות מגמה אמיתית.

---

## חלק ה' — נחמד-שיהיה (רק אם אפשר, לא קריטי)

- תיאור מתומצת על כל חברה + הסקטור שלה (מאיזה תחום היא)

---

## ⏸️ דחוי לסוף — לא נוגעים עכשיו

- כותרות חדשות לכל מנייה
- תיקון ה-cron האוטומטי
- תיקון המייל ל-2 כתובות

---

# 📋 סדר העבודה המומלץ

1. **קרא את כל הקבצים** ב-repo, ודא שהבנת את המבנה. שאל אותי שאלות.
2. **תיקונים קטנים** — מרקט קאפ 250M + צבעי רמזור (מהיר, נותן תחושת התקדמות)
3. **מצב היסטורי ב-scanner.py** — שיוכל לרוץ על תאריך בעבר, בלי באז
4. **הרצת 8 הסריקות ההיסטוריות** → מילוי Supabase
5. **חלונית הפרטים** — רוחב דינמי + הצגת כל ההיסטוריה
6. **לוגיקת "טופ 20 הכי תכופים"** — דירוג לפי מספר הופעות
7. **באז רק עליהם**
8. דחוי: תיאורי חברות, cron, מייל

---

# ⚠️ כללי עבודה חשובים

- **אל תתחיל לכתוב קוד עד שאני מאשר.** קודם תקרא, תבין, תשאל.
- **שנה דבר אחד בכל פעם.** אל תעשה את כל 8 השלבים בבת אחת — נעבוד צעד-צעד.
- **תסביר לי בעברית ובפשטות** מה אתה עושה ולמה. אני לא מתכנת מנוסה.
- **לפני כל commit** — תגיד לי מה השתנה ולמה.
- **אל תשבור את מה שעובד.** הסריקה השבועית, הדשבורד, השמירה ל-Supabase — כל זה עובד. בדוק שלא פגעת בזה.
- אם משהו לא ברור — **תשאל אותי**, אל תנחש.
- אני אצלם לך מסך / אשתף פלט כדי שתראה שהכל עובד.

---

# 🗂️ הקשר נוסף

הפרויקט פותח לאורך שיחה ארוכה בצ'אט אחר. ההחלטות העיקריות שהתקבלו שם:
- Reddit מוגבל ל-20 פוסטים למנייה, StockTwits ל-40 — איזון בין איכות אינדיקציה לעלות
- עברנו מ-APIs חינמיים ל-Apify כי החינמיים נחסמו מ-GitHub Actions
- הדשבורד מציג סנטימנט (%) ולא ספירות גולמיות, כי הספירות לא מדויקות (APIs מוגבלים בדגימה)
- ציון הבאז מחושב יחסית למרקט קאפ — מנייה קטנה עם הרבה באז = ציון גבוה; מנייה ענקית עם אותו באז = ציון נמוך

אם תצטרך פרטים נוספים על למה משהו בנוי כמו שהוא — תשאל אותי, אני אסביר.
